create database docket;
use database docket;

create table cartorios(
 id integer not null AUTO-INCREMENT,
 nome varchar(255) not null,
 PRIMARY KEY (id)
);

SELECT * FROM cartorios;

INSERT INTO cartorios (nome)

VALUES ("Nome1");

